# Componentes

## Estrutura:
- `common/` - Componentes comuns (Header, Footer, Button, etc.)
- `forms/` - Componentes de formulários
- `layout/` - Componentes de layout (Sidebar, Navigation, etc.)
- `pages/` - Componentes de páginas específicas
